steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/telos-orchestrator:latest'
      - '-f'
      - 'deploy/Dockerfile.cloudrun'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/telos-orchestrator:latest'
